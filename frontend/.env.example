# API base URL for backend requests.
# - "/api" = use Vite dev proxy (vite.config.js) and same origin in production (recommended).
# - "http://localhost:9080/api" = call backend directly in dev (ensure backend runs on 9080).
VITE_API_BASE_URL=/api
