# ===========================================
# CampusCart Backend - Environment Variables
# ===========================================

# ----- Database -----
# MongoDB connection string
MONGO_URL=mongodb://localhost:27017/campuscart

# ----- Redis -----
# Redis connection URI (used for refresh token storage)
# No auth: redis://localhost:6379
# With password: redis://:yourpassword@localhost:6379
REDIS_URI=redis://localhost:6379

# ----- JWT Secrets -----
# Use long, random strings in production (e.g. openssl rand -base64 32)
ACCESS_TOKEN_SECRET=your_access_token_secret_here
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here

# ----- Cloudinary (image uploads) -----
# Get these from https://console.cloudinary.com/
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ----- Server -----
# Port the backend runs on (default: 9080)
PORT=9080

# Environment: "development" or "production"
# Affects cookie security (secure flag) and static file serving
NODE_ENV=development
